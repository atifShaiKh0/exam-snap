import { Link } from "react-router-dom";

const Navbar = () => {
  return (
    <nav className="p-0 m-0 text-4xl bg-gray-300 flex flex-start space-x-6">
      This is a nav bar
    </nav>
  );
};

export default Navbar;
